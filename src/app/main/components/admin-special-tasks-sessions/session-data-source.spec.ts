import { SessionDataSource } from './session-data-source';

describe('SessionDataSource', () => {
  it('should create an instance', () => {
    expect(new SessionDataSource()).toBeTruthy();
  });
});
